const SearchBox = ({}) => {
  return (
    <>
      <div className="searchbar">
        <input className="search"></input>
        <button>Search</button>
      </div>
    </>
  );
};

export default SearchBox;
